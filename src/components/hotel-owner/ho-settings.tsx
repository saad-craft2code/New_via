"use client";

// Hotel Owner Settings — hotel-specific (check-in/out times, room status rules, etc.)
// Falls back to the shared settings shell.

export { BCSettings as HOSettings } from "@/components/bundle-creator/bc-settings";
