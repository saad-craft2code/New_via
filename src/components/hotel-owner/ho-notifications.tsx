"use client";

// Hotel Owner Notifications — hotel-specific (check-ins, room service, etc.)
// Falls back to the shared notifications list but with hotel-themed filters.

export { BCNotifications as HONotifications } from "@/components/bundle-creator/bc-notifications";
